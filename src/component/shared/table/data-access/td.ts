import { ReactElement } from 'react';
export interface TdProps {
    color: string;
    fontSize: string;
    fontWeight: string;
    padding: string;
    margin: string;
    textAlign: string;
    children?: ReactElement | ReactElement[]
  }
  